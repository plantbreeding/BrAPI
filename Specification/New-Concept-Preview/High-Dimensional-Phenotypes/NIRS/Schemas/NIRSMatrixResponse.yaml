info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
components:
  responses:
    NIRSMatrixResponse:
      content:
        application/json:
          schema:
            title: NIRSMatrixResponse
            properties:
              '@context':
                $ref: '#/components/schemas/Context'
              metadata:
                $ref: '#/components/schemas/metadata'
              result:
                properties:
                  instance:
                    $ref: '#/components/schemas/NIRSInstance'
                  data:
                    items:
                      $ref: '#/components/schemas/ObservationVector'
                    type: array
                required:
                - data
                type: object
            required:
            - metadata
            - result
            type: object
      description: OK
      headers: {}