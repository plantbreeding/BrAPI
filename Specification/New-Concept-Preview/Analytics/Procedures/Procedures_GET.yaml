info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths:
  /procedures:
    get:
      summary: Get the analysis procedures available on a server
      description: Get the analysis procedures available on a server
      parameters:
      - description: The unique identifier for a procedure
        in: query
        name: procedureDbId
        required: false
        schema:
          type: string
      - description: The human readable name of a procedure
        in: query
        name: procedureName
        required: false
        schema:
          type: string
      - $ref: '#/components/parameters/commonCropName'
      - $ref: '#/components/parameters/programDbId'
      - $ref: '#/components/parameters/externalReferenceId'
      - $ref: '#/components/parameters/externalReferenceSource'
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - $ref: '#/components/parameters/authorizationHeader'
      responses:
        '200':
          $ref: '#/components/responses/ProcedureListResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '401':
          $ref: '#/components/responses/401Unauthorized'
        '403':
          $ref: '#/components/responses/403Forbidden'
      tags:
      - Procedures
      - Analysis
components:
  responses:
    ProcedureListResponse:
      content:
        application/json:
          schema:
            title: ProcedureListResponse
            type: object
            properties:
              '@context':
                $ref: '#/components/schemas/Context'
              metadata:
                $ref: '#/components/schemas/metadata'
              result:
                type: object
                properties:
                  data:
                    type: array
                    description: The `data` array is part of the BrAPI standard List Response JSON container. `data` will always contain the primary list of objects being returned by a BrAPI endpoint. `data` is also the only array impacted by the `metadata.pagination` details. When the pagination parameters change, the `data` array will reflect those changes in the JSON response.
                    items:
                      $ref: '#/components/schemas/Procedure'
                required:
                - data
            required:
            - metadata
            - result
      description: OK
      headers: {}
      
