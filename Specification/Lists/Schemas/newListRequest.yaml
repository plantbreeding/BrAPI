components:
  schemas:
    newListRequest:
      properties:
        data:
          items:
            type: string
          type: array
        description:
          type: string
        listName:
          type: string
        listOwnerName:
          type: string
        listOwnerPersonDbId:
          type: string
        listSize:
          type: integer
        listSource:
          type: string
        listType:
          $ref: '#/components/schemas/listTypes'
      required:
      - listType
      type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
