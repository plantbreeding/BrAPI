components:
  schemas:
    listSummary:
      properties:
        dateCreated:
          format: date-time
          type: string
        dateModified:
          format: date-time
          type: string
        description:
          type: string
        listDbId:
          type: string
        listName:
          type: string
        listOwnerName:
          type: string
        listOwnerPersonDbId:
          type: string
        listSize:
          type: integer
        listSource:
          type: string
        listType:
          $ref: '#/components/schemas/listTypes'
      required:
      - listDbId
      - listType
      type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
