components:
  schemas:
    listDetails:
      allOf:
      - $ref: '#/components/schemas/listSummary'
      - properties:
          data:
            items:
              type: string
            type: array
        type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
