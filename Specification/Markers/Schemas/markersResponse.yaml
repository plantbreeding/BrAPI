components:
  schemas:
    markersResponse:
      properties:
        metadata:
          $ref: '#/components/schemas/metadata'
        result:
          properties:
            data:
              items:
                $ref: '#/components/schemas/marker'
              type: array
          type: object
      type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
