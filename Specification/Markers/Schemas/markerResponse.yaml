components:
  schemas:
    markerResponse:
      properties:
        metadata:
          $ref: '#/components/schemas/metadata'
        result:
          $ref: '#/components/schemas/marker'
      type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
