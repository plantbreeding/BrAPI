components:
  schemas:
    newListRequest:
      properties:
        additionalInfo:
          additionalProperties:
            type: string
          description: Additional arbitrary info
          type: object
        data:
          items:
            type: string
          type: array
        description:
          type: string
        listName:
          type: string
        listOwnerName:
          type: string
        listOwnerPersonDbId:
          type: string
        listSize:
          type: integer
        listSource:
          type: string
        listType:
          $ref: '#/components/schemas/listTypes'
      required:
      - listType
      type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
