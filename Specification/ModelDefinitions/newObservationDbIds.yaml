definitions:
  newObservationDbIds:
    properties:
      observations:
        items:
          properties:
            observationDbId:
              type: string
            observationUnitDbId:
              type: string
            observationVariableDbId:
              type: string
          type: object
        type: array
