definitions:
  newSampleDbId:
    properties:
      metadata:
        $ref: '#/definitions/metadata'
      result:
        properties:
          sampleDbId:
            type: string
        type: object
