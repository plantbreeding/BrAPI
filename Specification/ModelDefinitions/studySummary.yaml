definitions:
  studySummary:
    properties:
      active:
        type: boolean
      additionalInfo:
        type: object
        description: Additional arbitrary info
        additionalProperties:
          type: string
      endDate:
        format: date
        type: string
      locationDbId:
        type: string
      locationName:
        type: string
      seasons:
        items:
          type: string
        type: array
      startDate:
        format: date
        type: string
      studyDbId:
        type: string
      name:
        type: string
      studyType:
        type: string
      trialDbId:
        type: string
      trialName:
        type: string
