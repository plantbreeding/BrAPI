definitions:
  trait:
    properties:
      defaultValue:
        type: string
      description:
        type: string
      name:
        type: string
      observationVariables:
        items:
          type: string
        type: array
      traitDbId:
        type: string
      traitId:
        type: string
