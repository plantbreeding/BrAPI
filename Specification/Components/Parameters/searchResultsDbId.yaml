components:
  parameters:
    searchResultsDbId:
      description: Permanent unique identifier which references the search results
      in: path
      name: searchResultsDbId
      required: true
      schema:
        type: string
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
