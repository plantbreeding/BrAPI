components:
  parameters:
    authorizationHeader:
      description: "HTTP HEADER - Token used for Authorization \n\n<strong>Bearer {token_string} </strong>"
      example: Bearer XXXX
      in: header
      name: Authorization
      schema:
        pattern: ^Bearer .*$
        type: string
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
