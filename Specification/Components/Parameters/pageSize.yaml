components:
  parameters: 
    pageSize:
      name: pageSize
      description: The size of the pages to be returned. Default is `1000`.
      example: 1000
      in: query
      required: false
      schema:
        type: integer
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}