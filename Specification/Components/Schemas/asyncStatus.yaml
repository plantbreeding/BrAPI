components:
  schemas:
    asyncStatusMetadata:
      properties:
        asyncStatus:
          properties:
            asyncId:
              description: The unique ID to poll for asynchronous results
              type: string
            endTime:
              format: date
              type: string
            percentComplete:
              maximum: 100
              minimum: 0
              type: integer
            startTime:
              format: date
              type: string
            status:
              enum:
              - STARTED
              - PENDING
              - FINISHED
              - ERROR
              type: string
          required:
          - asyncId
      type: object
info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
