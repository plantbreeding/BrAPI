info:
  title: BrAPI
  version: ''
openapi: 3.0.0
paths: {}
components:
  schemas:
    CallSet:
      description: A CallSet is a collection of calls that were generated by the same analysis of the same sample.
      type: object
      properties:
        additionalInfo:
          additionalProperties:
            type: string
          description: Additional arbitrary info
          type: object
        sampleDbId:
          description: The Biosample entity the call set data was generated from.
          type: string
        created:
          description: The date this call set was created in milliseconds from the epoch.
          type: integer
        callSetDbId:
          description: The call set ID.
          type: string
        callSetName:
          description: The call set name.
          type: string
        updated:
          description: The time at which this call set was last updated in milliseconds from the epoch.
          type: integer
        studyDbId:
          description: The ID which uniquely identifies a study within the given database server
          type: string
        variantSetIds:
          description: The IDs of the variant sets this call set has calls in.
          items:
            type: string
          type: array

